{
  "scripts": {
    "analyze": "npm run build && npx webpack-bundle-analyzer build/static/js/*.js",
    "build:analyze": "npm run build && echo 'Build completed. Run npm run analyze to view bundle analysis'",
    "perf:dev": "REACT_APP_PERFORMANCE_MONITORING=true npm start",
    "perf:build": "REACT_APP_PERFORMANCE_MONITORING=true npm run build",
    "lighthouse": "lighthouse http://localhost:3000 --view --chrome-flags='--headless'",
    "test:performance": "npm run build && serve -s build -l 3000 & sleep 5 && npm run lighthouse && kill %1"
  },
  "devDependencies": {
    "webpack-bundle-analyzer": "^4.10.1",
    "lighthouse": "^11.4.0",
    "serve": "^14.2.1"
  }
}
